# tiny_server 开发方案（KISS）

## 目标

1. 先做成稳定的最小 HTTP 服务器。
2. 再补并发和静态资源能力。
3. 最后再考虑日志、配置和基础安全。

## 阶段 1：夯实最小内核（1-2 天）

1. 把请求解析拆分成独立函数（请求行、headers）。
2. 统一响应构造（状态码、常用头、body）。
3. 路由改成清晰结构（避免 `match` 越来越长）。
4. 增加基础错误处理（坏请求返回 `400`）。

验收标准：

1. `GET /`、`/health`、`/index.html` 稳定可用。
2. 非法请求不会让进程崩溃。
3. 代码结构清晰，函数职责单一。

## 阶段 2：并发与连接处理（2-3 天）

1. 引入线程池（固定 worker 数）。
2. 每个连接作为任务投递给线程池。
3. 支持 Keep-Alive（可先做简单版本：限制请求数/超时）。
4. 给连接处理增加超时，防止慢连接占满资源。

验收标准：

1. 并发压测下（如 `ab`/`wrk`）能稳定响应。
2. 单个慢连接不会拖垮整体服务。

## 阶段 3：静态文件服务完善（1-2 天）

1. 支持目录映射（例如 `/static/*`）。
2. 按扩展名返回 `Content-Type`。
3. 防目录穿越（禁止 `..` 路径）。
4. 支持 `404` 页面和基础缓存头（`Cache-Control`）。

验收标准：

1. 常见静态文件可正确访问。
2. 路径穿越请求被拒绝。
3. 浏览器直接可用，不靠特殊 hack。

## 阶段 4：工程化最小闭环（1-2 天）

1. 增加配置（监听地址、端口、线程数）。
2. 访问日志 + 错误日志（先 stdout/stderr 即可）。
3. 增加基础测试：请求解析、路由、响应格式。
4. 提供 `run_dev.sh`、`run_prod.sh`（prod 用 `cargo run --release`）。

验收标准：

1. 新人看配置能直接启动。
2. 关键逻辑有测试兜底。
3. 线上/开发启动方式明确。

## 建议优先级（从现在开始）

1. 先做阶段 1（结构化 + 400 错误）。
2. 然后直接做阶段 2（线程池并发），这是性能和稳定性的关键。
3. 最后补阶段 3、4。

## 这周可落地版本（MVP v0.2）

1. 并发处理 + 基础请求解析 + 静态目录服务。
2. 基本日志 + 简单配置。
3. 10-20 个并发请求稳定通过。
